{
  "name": "linkedin-agent-telegram-bot",
  "version": "1.0.0",
  "description": "Operator control panel for the LinkedIn content automation system. Accepts /pause, /resume, /pauseall, /status commands and triggers corresponding n8n webhooks.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "telegraf": "^4.16.3",
    "node-fetch": "^3.3.2",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
